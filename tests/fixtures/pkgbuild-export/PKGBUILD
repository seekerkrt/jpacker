# Maintainer: jpacker test fixture <fixture@example.invalid>
pkgname=('clean-root' 'clean-root-cli')
pkgver=1.0
pkgrel=1
pkgdesc='PKGBUILD export fixture: quotes, UTF-8 日本語, and exact bytes'
arch=('any')
source=("fixture::https://example.invalid/source?value='quoted'&other=1")
sha256sums=('SKIP')

package_clean-root() {
    printf '%s\n' "fixture $pkgver" > "$pkgdir/exported.txt"
}


